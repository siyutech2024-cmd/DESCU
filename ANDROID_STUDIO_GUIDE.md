# Android Studio 完整操作指南

## 第一步：打开项目

### 方法1：使用命令行
```bash
cd /Users/ishak/Downloads/descu---二手智选
./android-tools.sh open
```

### 方法2：直接在Android Studio中打开
1. 启动 **Android Studio**
2. 点击 **Open**
3. 导航到：`/Users/ishak/Downloads/descu---二手智选/android`
4. 点击 **OK**

---

## 第二步：等待项目同步

首次打开时，Android Studio会自动：
- 下载Gradle依赖
- 同步项目文件
- 索引代码

**进度显示**：查看底部状态栏，等待 "Gradle Build" 完成

⏱️ 首次同步可能需要5-10分钟

---

## 第三步：构建签名APK/AAB

### 3.1 创建签名密钥（首次构建）

1. 点击菜单 **Build** → **Generate Signed Bundle / APK...**

2. 选择类型：
   - **Android App Bundle (AAB)** - 推荐用于Google Play
   - **APK** - 用于直接安装

3. 点击 **Create new...**（创建新密钥库）

4. 填写密钥库信息：
   ```
   Key store path: /Users/ishak/descu-keystore.jks
   Password: [输入强密码，务必记住！]
   
   Key:
   Alias: descu-key
   Password: [相同或不同密码]
   Validity (years): 25
   
   Certificate:
   First and Last Name: [您的姓名]
   Organizational Unit: Development
   Organization: Venya Marketplace
   City or Locality: [城市]
   State or Province: [省份]
   Country Code (XX): MX
   ```

5. 点击 **OK**

⚠️ **超级重要**：
- 密钥库文件和密码是唯一凭证
- 备份 `descu-keystore.jks` 到安全位置
- 记录密码在密码管理器中

### 3.2 使用现有密钥构建

1. **Build** → **Generate Signed Bundle / APK...**

2. 选择 **Android App Bundle** 或 **APK**

3. 点击 **Choose existing...**，选择密钥库文件

4. 输入密码信息：
   - Key store password: [密钥库密码]
   - Key alias: descu-key
   - Key password: [密钥密码]

5. 点击 **Next**

6. 选择构建选项：
   - **Destination Folder**: 保持默认或选择位置
   - **Build Variants**: 选择 **release**
   - **Signature Versions**: 
     - ✅ V1 (Jar Signature)
     - ✅ V2 (Full APK Signature)

7. 点击 **Finish**

### 3.3 查看构建结果

构建完成后会弹出通知：

**APK位置**：
```
android/app/build/outputs/apk/release/app-release.apk
```

**AAB位置**：
```
android/app/build/outputs/bundle/release/app-release.aab
```

点击通知中的 **locate** 可直接打开文件位置

---

## 第四步：运行调试版本

### 4.1 连接设备

**真机**：
1. 在手机上启用开发者选项和USB调试
2. 用USB连接到电脑
3. 手机上授权USB调试

**模拟器**：
1. 点击 **Device Manager**（设备管理器）
2. 点击 **Create Device**
3. 选择设备型号（推荐Pixel 6）
4. 选择系统镜像（推荐API 33）
5. 点击 **Finish**
6. 点击 ▶️ 启动模拟器

### 4.2 运行应用

1. 在顶部工具栏选择设备
2. 点击绿色的 **Run** ▶️ 按钮（或按 Shift+F10）
3. 应用会自动安装并启动

---

## 第五步：常用操作

### 清理项目
```
菜单: Build → Clean Project
```
用途：删除旧的构建文件

### 重新构建
```
菜单: Build → Rebuild Project
```
用途：完全重新编译

### 查看日志
```
底部: Logcat 标签
```
过滤器：选择应用包名 `com.venya.marketplace`

### 调试
```
点击: Debug 🐞 按钮（在Run按钮旁边）
```
可以设置断点调试Java/Kotlin代码

### 查看APK内容
```
菜单: Build → Analyze APK...
```
选择生成的APK，查看大小、资源等

---

## 第六步：解决常见问题

### 问题1：Gradle同步失败

**解决方案**：
```
File → Invalidate Caches → Invalidate and Restart
```

### 问题2：设备未检测到

**真机检查**：
```bash
# 在终端执行
adb devices
```
应该显示您的设备

**解决**：
- 重新插拔USB
- 选择"文件传输"模式
- 重启adb: `adb kill-server && adb start-server`

### 问题3：构建失败

1. 查看 **Build** 标签页的错误信息
2. 点击错误可以跳转到源码位置
3. 常见错误：
   - 依赖版本冲突
   - ProGuard配置错误（已修复）
   - 内存不足

### 问题4：应用无法安装

**解决**：
```bash
# 卸载旧版本
adb uninstall com.venya.marketplace

# 重新安装
./android-tools.sh run
```

---

## 快捷键参考

| 操作 | Mac快捷键 | 说明 |
|------|-----------|------|
| 运行 | ⇧⌘R 或 Shift+F10 | 运行应用 |
| 调试 | ⌃D 或 Shift+F9 | 调试模式运行 |
| 停止 | ⌘F2 | 停止运行 |
| 查找 | ⌘F | 在当前文件查找 |
| 全局查找 | ⇧⌘F | 在整个项目查找 |
| 最近文件 | ⌘E | 最近打开的文件 |
| 构建 | ⌘F9 | 编译项目 |

---

## 构建类型对比

| 类型 | 用途 | 签名 | 优化 | 大小 |
|------|------|------|------|------|
| **Debug** | 开发测试 | 自动签名 | 无 | 大 |
| **Release** | 发布生产 | 需要密钥 | R8压缩 | 小 |

---

## 发布到Google Play

### 准备清单

- [x] 已构建签名的AAB
- [ ] 创建Google Play Console账号（$25一次性费用）
- [ ] 准备应用图标（512x512px）
- [ ] 准备屏幕截图（至少2张）
- [ ] 编写应用描述
- [ ] 设置隐私政策URL
- [ ] 完成内容分级问卷

### 上传流程

1. 访问 [Google Play Console](https://play.google.com/console)
2. 点击 **创建应用**
3. 填写基本信息
4. 上传AAB文件
5. 填写商店详情
6. 提交审核

---

## 小贴士

💡 **推荐工作流程**：
1. 开发时用 Debug 版本快速测试
2. 发布前用 Release 版本验证
3. AAB 发布到 Google Play
4. APK 用于直接分发

💡 **性能优化**：
```
File → Settings → Build, Execution, Deployment → Compiler
勾选: Compile independent modules in parallel
增加: --max-workers=4
```

💡 **GPU加速模拟器**：
```
Tools → AVD Manager → 编辑设备
Graphics: Hardware - GLES 2.0
```

---

## 需要帮助？

如果遇到问题：
1. 查看 **Build Output** 标签页
2. 查看 **Logcat** 日志
3. Google搜索错误信息
4. 告诉我具体错误，我会帮您解决！

---

**祝您构建成功！** 🚀
